Submission artifacts generated by GitHub Copilot assistant

What I did (automated steps):
- Created a local Python virtual environment at `dealership/server/.venv`.
- Installed Python dependencies from `dealership/server/requirements.txt` and microservices requirements.
- Installed Django into the venv and ran `manage.py check` (no issues reported).
- Attempted `npm install` for Node components; environment may not have Node/npm so that step can fail locally.
- Converted all `.md`, `.markdown`, and `.html` files under `dealership/` to plain text copies in `submission_artifacts/plain_text/`.

How to reproduce (commands run):
1. Create venv and install Python deps (from repository root):

   cd dealership/server
   python -m venv .venv
   .venv\Scripts\python -m pip install --upgrade pip setuptools wheel
   .venv\Scripts\python -m pip install -r requirements.txt

2. Run Django checks:

   .venv\Scripts\python manage.py check

3. Convert markdown/html to plain text (already produced):

   .venv\Scripts\python tools/convert_md_to_text.py

Notes:
- All generated plain-text files are in `submission_artifacts/plain_text/`.
- If you want Node dependencies installed, run `npm install` in `dealership/server/database` and `dealership/server/frontend` on a machine with Node.js installed.
